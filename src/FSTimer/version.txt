version3.1
加入了文件拖入的功能。

version3
1. 新版本对代码进行了大改，代码结构更加清晰。
2. 中文界面。
3. 取消了按T输入命令、按H显示帮助的功能，不再显示提示文本。
4. 支持更改窗口大小，能够根据窗口大小正确完整地显示全部记录，不需要限制记录的数量。
5. 大幅优化可视化效果，增加惯性、弹性碰撞等运动效果。
6. Viewer文件支持把存储文件拖上去直接打开。

version2.1
1. 新版本部分改善了可视化效果。
2. 允许使用最大化按钮。
3. 大幅提高了代码的执行频率，使得10ms的计时精度实至名归。

version2
1. 新版本使FSTimer.pyw和FSViewer.pyw的CPU占用降到原来的约十分之一。
2. 重写了deflate.py和restore.py，改bug的同时让代码更加可读。

